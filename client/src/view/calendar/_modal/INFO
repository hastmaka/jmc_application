come from client - select client by default
if RBT - usually have one client (if not, let rbt select the client - field empty)
if Analist - field empty (let her/him select)
if (Supervision) - self

restriction:
no more that 6 hours dairy
weekly 30 hours
rbt max 60 hours case (total)
